# 联调执行记录（2026-05-07）

> 说明：原文件未能完整取回，以下为依据 TODO、运行状态文件与联调日志重建的摘要记录。
>
> 恢复完整度：中高。已结合 `tmp\run-control` 运行状态与日志证据补充了更多可核对信息。

## 1. 记录目的

用于说明在 2026-05-07 的真实 async 环境中，项目已完成一轮更接近答辩环境的复测。

## 2. 本轮重点

- 真实 async 模式复测
- Redis + API + Worker + Frontend + Juice Shop 联动验证
- PDF 导出依赖补齐后的再验证

## 3. 环境与运行证据

### 3.1 启动方式

- 使用 `start_demo_env.ps1` 统一启动演示环境
- 运行状态文件：`tmp/run-control/demo-runtime.json`
- 日志目录：`tmp/run-control/logs`

### 3.2 关键日志证据

- `api.err.log` 显示 Flask 已监听 `http://127.0.0.1:5000`
- `worker.out.log` 显示 Celery 已注册：
  - `app.workers.dast_tasks.execute_dast_task`
  - `app.workers.sast_tasks.execute_sast_task`
- `worker.err.log` 显示：
  - `Connected to redis://localhost:6379/0`
  - `celery@... ready`
- `frontend.out.log` 显示 Vite 已就绪：`http://127.0.0.1:5173/`

### 3.3 健康检查口径

后端健康接口位于：`http://127.0.0.1:5000/api/health`

重点关注：

- `service`
- `database`
- `redis`
- `dast_scanner`
- `celery_mode`

## 4. 关键结论

- 已安装 `MSYS2.MSYS2` 与 `mingw-w64-x86_64-pango`
- WeasyPrint 在 Windows 下的原生依赖问题得到补齐
- SAST `49` 与 DAST `50` 的 PDF 导出均返回 `200`
- 真实异步链路已不再只停留在 eager 模式

补充判断：

- 这轮复测把“接口层能跑”进一步推进为“Redis + Worker + 扫描器 + 导出链路都实际参与”
- 对答辩与演示而言，这轮记录比 2026-04-22 的联调结果更有说服力

## 5. 对后续的意义

- 可作为答辩前环境一致性的重要依据
- 可作为“HTML / PDF 导出已验证”的最新记录
- 为实验材料与演示路径提供更强支撑

## 6. 仍需注意的风险

- 这轮记录证明了 async 与 PDF 导出可跑通，但不代表所有异常场景都已覆盖
- Windows 下 PDF 导出仍然对本机 GTK / Pango 环境敏感
- 答辩当天仍应优先准备已完成任务与现成导出样例，避免现场等待完整扫描